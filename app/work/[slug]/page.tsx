import {notFound} from 'next/navigation'; import {Detail} from '../../components'; import {work,findItem} from '../../data'; export function generateStaticParams(){return work.map(x=>({slug:x.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){let {slug}=await params;let item=findItem(work,slug);if(!item)notFound();return <Detail item={item} type="work"/>}
