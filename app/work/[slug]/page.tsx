import {notFound} from 'next/navigation'; import {work,findItem} from '../../data'; import WorkShowcase from '../work-showcase'; export function generateStaticParams(){return work.map(x=>({slug:x.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){let {slug}=await params;let item=findItem(work,slug);if(!item)notFound();return <WorkShowcase item={item}/>}
