import {notFound} from 'next/navigation'; import {Detail} from '../../components'; import {academy,findItem} from '../../data'; export function generateStaticParams(){return academy.map(x=>({slug:x.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){let {slug}=await params;let item=findItem(academy,slug);if(!item)notFound();return <Detail item={item} type="course"/>}
