import {NextRequest,NextResponse} from 'next/server';import {adminToken} from '../../../lib/backend';
export async function POST(request:NextRequest){const {password}=await request.json();if(!process.env.ADMIN_PASSWORD||password!==process.env.ADMIN_PASSWORD)return NextResponse.json({error:'Incorrect password.'},{status:401});const response=NextResponse.json({ok:true});response.cookies.set('upstride_admin',adminToken(),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'strict',maxAge:60*60*8,path:'/'});return response}
export async function DELETE(){const response=NextResponse.json({ok:true});response.cookies.set('upstride_admin','',{httpOnly:true,maxAge:0,path:'/'});return response}
