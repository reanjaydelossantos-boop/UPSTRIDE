import type {MetadataRoute} from 'next';import {academy,resources,solutions,work} from './data';export default function sitemap():MetadataRoute.Sitemap{let base='https://upstride.com';let fixed=['','solutions','academy','resources','work','about','contact','verify-certificate'];return [...fixed.map(x=>({url:`${base}/${x}`,lastModified:new Date()})),...solutions.map(x=>({url:`${base}/solutions/${x.slug}`})),...academy.map(x=>({url:`${base}/academy/${x.slug}`})),...resources.map(x=>({url:`${base}/resources/${x.slug}`})),...work.map(x=>({url:`${base}/work/${x.slug}`}))]}
