'use client';
import {useState} from 'react';import {ArrowRight,CheckCircle2,AlertCircle} from '../icons';
export function ContactForm(){const [state,setState]=useState<'idle'|'sending'|'success'|'error'>('idle'),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;if(!form.checkValidity()){setState('error');setError('Please check the highlighted required fields.');form.reportValidity();return}setState('sending');setError('');const data=Object.fromEntries(new FormData(form).entries());try{const response=await fetch('/api/inquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Submission failed');setState('success');form.reset()}catch(err){setState('error');setError(err instanceof Error?err.message:'We could not send your inquiry.')}}if(state==='success')return <div className="form status" role="status"><CheckCircle2/><h2>Thanks—your inquiry is with us.</h2><p>We’ve saved your consultation brief. The Upstride team can now review it in the secure admin workspace.</p><button className="textlink" onClick={()=>setState('idle')}>Send another inquiry</button></div>;return <form className="form" onSubmit={submit} noValidate><div className="formhead"><small>CONSULTATION BRIEF</small><span>Fields marked * are required</span></div>{state==='error'&&<div className="error" role="alert"><AlertCircle/>{error}</div>}<div className="fields"><label>Full name *<input name="name" autoComplete="name" required placeholder="Your name"/></label><label>Work email *<input type="email" name="email" autoComplete="email" required placeholder="you@company.com"/></label><label>Business / company<input name="company" autoComplete="organization" placeholder="Company name"/></label><label>I&apos;m interested in *<select name="interest" required defaultValue=""><option value="" disabled>Select one</option><option>UPSTRIDE Solutions</option><option>UPSTRIDE Academy</option><option>Resources</option><option>Partnership / other</option></select></label><label>Indicative budget<select name="budget" defaultValue=""><option value="">Prefer to discuss</option><option>Under ₱50,000</option><option>₱50,000–₱150,000</option><option>₱150,000–₱500,000</option><option>₱500,000+</option></select></label><label>Ideal timeline<select name="timeline" defaultValue=""><option value="">Select timeline</option><option>As soon as practical</option><option>1–2 months</option><option>3–6 months</option><option>Exploring for later</option></select></label><label className="full">What would forward look like? *<textarea required name="message" rows={5} placeholder="Goals, challenges, current setup, or anything useful to know…"/></label></div><label className="consent"><input type="checkbox" required/> <span>I agree to be contacted about this inquiry. *</span></label><button className="btn" type="submit" disabled={state==='sending'}>{state==='sending'?'Sending securely…':<>Send my consultation <ArrowRight size={17}/></>}</button><p className="demo-note">Securely submitted to the Upstride inquiry workspace.</p></form>}
