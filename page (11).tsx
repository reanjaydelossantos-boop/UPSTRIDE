import {notFound} from 'next/navigation'; import {Detail} from '../../components'; import {findItem,solutions} from '../../data'; export function generateStaticParams(){return solutions.map(x=>({slug:x.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){let {slug}=await params;let item=findItem(solutions,slug);if(!item)notFound();return <Detail item={item} type="solution"/>}
