import {notFound} from 'next/navigation'; import {Detail} from '../../components'; import {resources,findItem} from '../../data'; export function generateStaticParams(){return resources.map(x=>({slug:x.slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){let {slug}=await params;let item=findItem(resources,slug);if(!item)notFound();return <Detail item={item} type="resource"/>}
